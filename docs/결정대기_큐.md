# 중요 결정 대기 큐 (사용자 판단 필요)

자율 개발 중 발생한, **오너의 결정이 필요한 중요 항목**을 여기 쌓는다.
개발은 이 결정들과 무관한 부분부터 진행하여 **멈추지 않는다.** 기상 후 위에서부터 검토 요청.

범례: 🔴 개발 방향을 크게 가름 · 🟡 확인 필요 · 🟢 참고

---

## ✅ Q-1. 오디오 확보 방식 — **(a) tabCapture 실시간 채택 (2026-07-19, 사용자 결정)**
- 결정 D-7/D-18 반영. 실시간 캡처→STT→MT→오버레이 경로로 진행.
- 후속으로 (a) 내부 서브결정 Q-4 발생 → 아래.

## ✅ Q-4. (a) 내부 STT 실행 위치 — **온디바이스(WASM/WebGPU) 채택 (2026-07-19, 사용자)**
- 결정: 온디바이스(Transformers.js/Whisper). 오디오가 기기 밖으로 안 나가 프라이버시·ToS 유리, 서버비용 0.
- 검증: 브라우저 내 중국어 실시간 인식 가능(WebGPU 시 실시간 5~8배속) — `docs/재생환경_호환성.md`.
- 처리: `OnDeviceSttClient` 스켈레톤 작성(인터페이스 뒤). 실기기(로컬/WebGPU) 정확도·지연 실측 남음.

## 🟡 Q-5. 서드파티 네이티브 유튜브 래퍼 앱 지원 범위
- **배경**: 공식 유튜브 PC 앱은 없음. PC "앱"은 PWA(크로미엄)라 확장이 커버됨(단축키로 시작). 그러나 **서드파티 네이티브 래퍼 앱**은 확장이 못 미침 → 지원하려면 시스템 오디오 루프백 + 오버레이 창의 **별도 데스크톱 앱**(OS별) 필요.
- **제안**: MVP는 **브라우저 탭 + PWA 한정**, 서드파티 네이티브 앱은 범위 밖(향후 2단계). 
- **요청**: 이 범위 설정 승인 여부. (미정이면 브라우저+PWA 한정으로 진행)

## 🟡 Q-2. 번역 품질 개선 우선순위 — 고유명사 용어집 조기 도입 여부
- **배경**: 파일럿 ②에서 고유명사 오류(미란→밀라노, 커모호→코모 호수, AD)가 주요 약점으로 드러남. 계획서는 용어집을 "MVP 이후"로 뒀으나, 실측상 체감 품질에 큰 영향.
- **현재 처리(자료구조만 마련, 기본 off)**: `pilot/glossary/`에 용어집 로더·적용(`Glossary`)과 시드(`seed.json`)를 구현. 파일럿 파이프라인에 **opt-in**(`--glossary`)으로 통합, 기본은 비활성. 시드로 파일럿 ② 오류 5개(미란/커모호/모커호/AD라디오/디자인위원회) **교정 확인**.
- **요청**: 용어집을 **MVP 기본 활성**으로 앞당길지(품질↑), 계획대로 이후로 둘지. (미정이면 계속 opt-in 유지하며 시드만 점진 확장)

## 🟡 Q-3. MVP 자막 공급 방식(초기) — 실시간 처리 전 '사전 생성 SRT 로드'로 시작?
- **배경**: (a)/(b) 미정 상태에서 오버레이를 실사용 데모하려면, 우선 **사전 생성된 SRT를 불러와 표시**하는 경로가 필요(캐싱 설계와도 정합).
- **현재 처리**: 정적 SRT 로드 공급원을 기본 구현 중(안전·즉시 데모 가능). 실시간 공급원은 (a)/(b) 결정 후.
- **요청**: 이 접근(사전 생성 SRT 우선) 승인 여부. (기본값으로 진행 중, 반대 시 조정)

---

## 처리 로그
- 2026-07-19: 큐 신설. Q-1~Q-3 등록. 오버레이 레이어 개발은 이 결정들과 독립적으로 진행 시작.
