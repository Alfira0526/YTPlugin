# === P0 파일럿 하네스 의존성 ===
# 스모크 경로(python -m pilot.run_pilot --smoke)는 아래 의존성 없이도 동작한다(순수 파이썬).
# 실제 백엔드를 쓸 때만 해당 그룹을 설치한다.

# --- MT: DeepL (1차) ---
deepl>=1.18

# --- MT: Papago (대안) / 기타 HTTP ---
requests>=2.31

# --- STT: SenseVoice (대안) ---
# funasr>=1.1
# torch>=2.2          # CPU 빌드 권장 (GPU 있으면 CUDA 빌드)
# torchaudio>=2.2

# --- STT: Qwen3-ASR (1차) ---
# transformers>=4.44
# torch>=2.2
# torchaudio>=2.2
# accelerate>=0.30
#
# ⚠️ funasr/transformers는 최초 실행 시 HuggingFace/ModelScope에서 가중치를 받는다.
#    HF/ModelScope 아웃바운드가 허용된 환경(로컬/GPU 등)에서만 설치·실행할 것.
