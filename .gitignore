# === Secrets / env ===
.env
.env.*
!.env.example
*.key

# === Python ===
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pipprobe/
*.egg-info/
.pytest_cache/

# === Models / weights (never commit — fetched at runtime) ===
models/
*.pt
*.onnx
*.bin
*.safetensors

# === Pilot artifacts (local test data & outputs) ===
pilot/samples/*.wav
pilot/samples/*.mp3
pilot/samples/*.m4a
pilot/samples/*.mp4
pilot/outputs/
*.srt
!pilot/samples/.gitkeep

# === Subtitle cache (runtime) ===
cache/*.json
!cache/README.md

# === OS / editor ===
.DS_Store
*.swp
.idea/
.vscode/
